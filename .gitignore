# Ignore everything by default
*

# But not these directories (allows recursion into subdirectories)
!*/

# Keep these file types
!*.py
!*.yaml
!*.yml

# Keep .gitignore itself
!.gitignore

# Explicitly ignore sensitive files (even though * already covers them)
# SSH Keys
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_*
*.pem
*.key

# API Keys and Secrets
.env
.env.*
*_secret
*_token
api_key*
*api_key*
secret_*
token_*

# Credentials
credentials
credentials.json
config.json
